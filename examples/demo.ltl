// examples/demo.ltl — runnable demo for ltl_linsolve

import std::io
use ltl_linsolve as lib

fn main() {
    io::println("ltl_linsolve demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}