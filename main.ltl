// ltl-linsolve/main.ltl
//
// Gaussian elimination + LU decomposition solver.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-linsolve v0.1.0")
    io::println("Gaussian elimination + LU decomposition solver.")
    0
}
